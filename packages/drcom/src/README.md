---------------
### Simple ZJUT drcom client dirty hack C-version

需要修改drcom.conf 文件中 username(帐号), password(密码), mac(MAC 地址, 01:02:03:04:05:06 格式) 等参数，编译
	
	make all
	sudo make install


直接在终端运行 drcom 
